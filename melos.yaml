name: easy_task_app

packages:
  - core
  - design_system
  - design_system/easy_task_widgetbook
  - internationalization
  - easy_task_app

command:
  bootstrap:
    hooks:
      pre: melos run clean

scripts:
  lint:all:
    run: melos run analyze && melos run format
    description: Run all static analysis checks.

  analyze:
    run: dart analyze . --fatal-infos
    exec:
      concurrency: 2
    description: |
      Run dart analyze in all packages.
       - Note: you can also rely on your IDEs Dart Analysis / Issues window.

  format:
    run: dart format .
    exec:
      concurrency: 2
    description: Formats the code of all packages (Dart).

  test:
    run: "flutter test"
    exec:
      failFast: true
    description: Run flutter test for a specific package.
    packageFilters:
      dirExists:
        - test
      ignore:
        - "web"
        - "odm"
        - "example"

  clean:
    exec: flutter clean

  pub:get:
    exec: flutter pub get

  build_runner:build:
    exec: flutter pub run build_runner build --delete-conflicting-outputs
    packageFilters:
      dependsOn: build_runner

  build_runner:watch:
    exec: flutter pub run build_runner watch --delete-conflicting-outputs
    packageFilters:
      dependsOn: build_runner

  flutter:run:
    exec: flutter run

  app:
    description: Bootstrap and run the Easy Task App with environment variables on your device. Remember to replace <YOUR-DEVICE-ID> with the id from your device, you can get it by running flutter devices.
    run: |                                            # Your device ID goes here
      melos bootstrap && cd easy_task_app && flutter run -d <YOUR-DEVICE-ID> --dart-define=SUPABASE_URL=https://nzjojzchnyihkehntoxw.supabase.co --dart-define=SUPABASE_API_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im56am9qemNobnlpaGtlaG50b3h3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTM1MjM3MjYsImV4cCI6MjA2OTA5OTcyNn0.Ned5voI11QDLjinkMgw00Gb3jMi731_cXr_IBAmkdCs
