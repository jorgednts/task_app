name: Auto Assigner
on: [pull_request]

jobs:
  label:

    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: toshimaru/auto-author-assign@v2.1.1
