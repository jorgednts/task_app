name: core
description: "EasyTask App Core Layer"

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.1.4
  logger: ^2.6.1
  image_picker: ^1.1.2
  permission_handler: ^12.0.1
  path: ^1.9.1
  mime: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
